import 'package:flutter/material.dart';

class AnniversaryCard extends StatelessWidget {
  const AnniversaryCard({super.key, required String date, required String});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('card'),
    );
  }
}
